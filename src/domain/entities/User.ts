/* eslint-disable no-unused-vars */
export class User {
    constructor(
        public readonly id: string,
        public name: string,
        public email: string,
        public address: string,
        public phone: number,
    ) {}
}
